# bo

## 수정 파일 위치
- `app.js` : 화면/컴포넌트 렌더링 로직 (이번 UI 수정 파일)
- `index.html` : 앱 엔트리 HTML
- `styles.css` : 스타일 정의

## 로컬 미리보기
아래 명령으로 로컬 서버를 띄운 뒤 브라우저에서 확인하세요.

```bash
cd /workspace/bo
python3 -m http.server 4173
```

브라우저 접속:
- `http://localhost:4173`
